package com.truesight.backend.domain;

/** AC 9.3's required action-type taxonomy for the AI audit trail. */
public enum AuditActionType {
    SEC_FILING_EXTRACTION,
    NEWS_SENTIMENT_ANALYSIS,
    SIMULATION_PROPAGATION,
    GRAPH_QUESTION_ANSWERING
}
